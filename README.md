# MiniReaderNative

基于开源项目 `hhuua123/Book_Reader` 改造的 iOS 原生小说阅读器实验版。

目标：

- 保留原项目的书架、搜索、详情、目录、正文阅读 UI。
- 去掉已经失效的 `api.hhuua.top` 后端依赖。
- 后续接入可维护的书源解析层。
- 使用 GitHub Actions 构建 TrollStore 可安装 IPA。

下载地址：Release `native-latest`。
